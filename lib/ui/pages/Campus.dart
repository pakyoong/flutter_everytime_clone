import 'package:flutter/material.dart';

class CampusPick extends StatelessWidget {
  const CampusPick({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text(
          'campus pick page',
        ),
      ),
    );
  }
}